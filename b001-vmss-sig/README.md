# VM Scale Sets + Shared Image Gallery
This demo is an environment which deploys various resources for a sample application built with virtual machine scale sets, pulling from the shared image gallery

## Steps
